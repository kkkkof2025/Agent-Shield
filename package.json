{
  "name": "agent-shield-runtime",
  "version": "0.1.0",
  "description": "Local AI CLI runtime wrapper for session recovery, auditing, and safety boundaries.",
  "license": "UNLICENSED",
  "private": true,
  "type": "commonjs",
  "bin": {
    "agent-shield": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "agent-shield": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3"
  }
}

